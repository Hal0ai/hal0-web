# build output
dist/
.astro/

# deps
node_modules/

# environment
.env
.env.*
!.env.example

# logs
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*
yarn-error.log*

# editors
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
.idea/

# macOS / desktops
.DS_Store
Thumbs.db

# deploy targets (site deploys via Vercel; local CLI state stays out either way)
.vercel
.netlify

# misc
*.log
.cache/

# Playwright MCP scratch (snapshots/screenshots from in-browser verification)
.playwright-mcp/
# stray capture artifacts dropped at repo root (real assets live in public/screenshots/)
/*.png

# orchestration scratch (style guides, agent notes, etc.)
.claude/

# Serena MCP cache
.serena/

# graphify knowledge-graph output
graphify-out/

# Superset personal overrides (team config in .superset/config.json is committed)
.superset/config.local.json

# benchmark data source (regenerates src/data/model-roster.ts via bench/build_data.py)
!bench/roster.log
