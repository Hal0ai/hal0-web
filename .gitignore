# build output
dist/
.astro/

# deps
node_modules/

# environment
.env
.env.*
!.env.example

# logs
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*
yarn-error.log*

# editors
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
.idea/

# macOS / desktops
.DS_Store
Thumbs.db

# deploy targets (we don't deploy to these — keep out anyway)
.vercel
.netlify

# misc
*.log
.cache/

# orchestration scratch (style guides, agent notes, etc.)
.claude/

# Serena MCP cache
.serena/
.claude/wip.json

# knowledge graph (graphify) — cache/state are local-only churn; ignore
# everything by default and re-include only the artifacts we track.
graphify-out/*
!graphify-out/GRAPH_REPORT.md
!graphify-out/manifest.json
!graphify-out/graph.json
