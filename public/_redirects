# Cloudflare Pages redirects/rewrites.
# https://developers.cloudflare.com/pages/configuration/redirects/
#
# Host-conditional rewrite for releases.hal0.dev is handled by
# `functions/_middleware.ts` instead — `_redirects` host rules
# don't reliably fire for same-project subdomain rewrites.
#
# Add path-based redirects here as needed.

# hal0 README + older links point at hal0.dev/roadmap; the roadmap
# lives as a landing-page section, not a standalone page.
/roadmap  /#roadmap  302

# The Quick Start & Migration Guide is authored in the product repo as
# docs/hal0-install-migration-guide.html and hosted here verbatim at
# /install-migration-guide/ — catch links that use the source filename.
/hal0-install-migration-guide.html        /install-migration-guide/  301
/docs/hal0-install-migration-guide.html   /install-migration-guide/  301
