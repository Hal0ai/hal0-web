# Cloudflare Pages redirects/rewrites.
# https://developers.cloudflare.com/pages/configuration/redirects/
#
# Host-conditional rewrite for releases.hal0.dev is handled by
# `functions/_middleware.ts` instead — `_redirects` host rules
# don't reliably fire for same-project subdomain rewrites.
#
# Add path-based redirects here as needed.
