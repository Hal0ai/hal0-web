---
title: OpenWebUI as the chat tab
description: An honest note on running Open WebUI against hal0 — what works out of the box, and what still needs a manual pointer.
kbCategory: tool reviews
reviewed: "2026-08-09"
---

import { Aside } from '@astrojs/starlight/components';
import KbStamp from '../../../../components/KbStamp.astro';

<KbStamp />

Open WebUI is the chat UI hal0 ships an installer profile for, and it's
the tool reviews category's first honest note: what it gets right, and
where the seams still show.

## What works without configuration

Point Open WebUI's OpenAI-compatible connection at `http://localhost:8080/v1`
and it lists every hal0 slot as a model, `hal0/agent` included. Streaming,
tool calls, and the Call mode voice path (STT in, TTS out) all work
through the same connection — no separate integration for voice.

## What still needs a manual step

- **Model switching mid-conversation** works, but swapping to a slot that
  isn't currently resident pays the cold-start cost inline — Open WebUI
  doesn't pre-warm on hover. If you're demoing, warm the slot first with
  a throwaway request.
- **Per-slot system prompts** live in hal0's slot config, not Open
  WebUI's per-model settings — set them once at the slot, not per chat.

<Aside type="tip" title="Reranker and embed slots don't show up here">
Open WebUI's model list only shows `type=llm` slots. Embed and rerank
slots power its RAG features under the hood (set them in Admin Settings →
Documents) — they're intentionally absent from the chat model picker.
</Aside>

## Verdict

For a self-hosted chat front end that needs zero glue code against hal0's
`/v1/*` surface, it's the right default — that's why the installer offers
it. The rough edges above are minor and mostly about knowing where a
setting lives, not missing functionality.

## Related

- [Talking to /v1/chat/completions](/docs/reference/api/openai-compat/)
- [Connect MCP servers](/docs/guides/connect-mcp/)

<Aside type="note" title="More tool reviews are coming">
Continue.dev against a coder slot and a look at which MCP servers are
worth running are planned pages in this category — not yet written. This
page is the category's seed.
</Aside>
