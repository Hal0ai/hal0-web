---
title: Your first slot
description: The shortest honest path from a bare hal0 install to one warm, serving slot answering a request.
kbCategory: getting started
reviewed: "2026-08-09"
---

import { Steps } from '@astrojs/starlight/components';
import KbStamp from '../../../../components/KbStamp.astro';

<KbStamp
  category="getting started"
  reviewed="2026-08-09"
  editHref="https://github.com/hal0ai/hal0-web/edit/master/src/content/docs/kb/getting-started/your-first-slot.mdx"
/>

Installing hal0 gets you a running daemon with nothing loaded — a slot is
what actually holds a model in memory and answers requests. This page is
the fast path from "installed" to "one slot, warm, serving." For the full
install walkthrough (drivers, GTT sizing, package prerequisites), see
[Install on bare metal](/docs/getting-started/bare-metal/) — this page
assumes that part is already done.

<Steps>

1. **Confirm the daemon is up.**

   ```sh
   hal0 status
   ```

   A healthy install reports the API listening on `:8080` and zero slots
   loaded — that's expected on a fresh box.

2. **Pull a small model first.** Don't reach for the biggest model in the
   roster on your first try — pull something that fits comfortably so a
   slow network or a tight memory budget isn't the thing you're debugging.

   ```sh
   hal0 model pull qwen3.5-9b-q4kxl
   ```

3. **Create a slot from it.**

   ```sh
   hal0 slot create agent --model qwen3.5-9b-q4kxl
   ```

   `agent` is the canonical role most tools default to — pointing a new
   slot at it means anything speaking to `hal0/agent` (Open WebUI, Hermes,
   a raw `curl`) works immediately with no further config.

4. **Talk to it.**

   ```sh
   curl localhost:8080/v1/chat/completions \
     -H 'Content-Type: application/json' \
     -d '{"model":"hal0/agent","messages":[{"role":"user","content":"hi"}]}'
   ```

   The first request pays the cold-start cost — `offline → starting →
   warming → ready → serving`. Subsequent requests to the same slot are
   fast.

</Steps>

## What comes next

- [Talking to /v1/chat/completions](/docs/reference/api/openai-compat/) —
  the full request/response shape, streaming, and tool calls.
- [Picking a loadout](/docs/guides/choose-models/) — sizing multiple
  co-resident slots (agent, embed, rerank, voice) instead of one.
- [Reading the memory map](/kb/operating-hal0/reading-the-memory-map/) —
  what "resident" actually costs once you have more than one slot loaded.
