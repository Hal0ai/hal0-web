---
/**
 * /docs/reference/ — section listing page. See
 * src/pages/docs/concepts/index.astro for why these live in `src/pages/`
 * rather than as `index.mdx` in the (mirror-generated) docs collection.
 */
import StarlightPage from '@astrojs/starlight/components/StarlightPage.astro';
import SectionPageList from '../../../components/SectionPageList.astro';
---

<StarlightPage
	frontmatter={{
		title: 'Reference',
		description:
			'Exact contracts — CLI, config schema, slot lifecycle, env vars, paths, MCP tools, hardware matrix, and the HTTP APIs.',
		tableOfContents: false,
	}}
>
	<p>
		Look-up pages: exhaustive, alphabetical where order doesn't matter, and
		honest about defaults. The API sub-pages document the OpenAI-compatible
		surface and the native REST endpoints.
	</p>
	<SectionPageList section="reference" />
</StarlightPage>
