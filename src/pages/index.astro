---
/**
 * Landing page — / (hal0.dev).
 *
 * hal0 v2 design — sodium amber on graphite. Strix Halo first, the rest
 * of the matrix honest. Claims and numbers trace to the hal0 codebase
 * (src/hal0) — verify there, not from a copy doc.
 */
import MarketingLayout from '../layouts/MarketingLayout.astro';
import LiveArtifact from '../components/landing/LiveArtifact.astro';

const canonicalUrl = 'https://hal0.dev/';
const version = 'v0.9.7';

const description =
  'hal0 is an open-source homelab AI inference platform built for the AMD Ryzen AI Max+ 395 — "Strix Halo" — with 128 GB of unified memory and an XDNA NPU. OpenAI-compatible /v1/* API, systemd-managed slots, prewired chat UI, in-dashboard benchmarks, signed self-update. One command installs the lot.';

const softwareApplicationLd = {
  '@context': 'https://schema.org',
  '@type': 'SoftwareApplication',
  name: 'hal0',
  applicationCategory: 'DeveloperApplication',
  operatingSystem: 'Linux',
  description,
  url: canonicalUrl,
  downloadUrl: 'https://hal0.dev/install.sh',
  license: 'https://www.apache.org/licenses/LICENSE-2.0',
  softwareVersion: version,
  codeRepository: 'https://github.com/hal0ai/hal0',
  keywords:
    'Strix Halo, Ryzen AI Max+ 395, AMD XDNA NPU, 128 GB unified memory, homelab AI, OpenAI-compatible API, local LLM, Proxmox LXC, llama.cpp, FLM, AI inference appliance',
  offers: { '@type': 'Offer', price: '0', priceCurrency: 'USD' },
  author: { '@type': 'Organization', name: 'hal0ai', url: 'https://github.com/hal0ai' },
};

const features = [
  {
    tag: '/v1/*',
    title: 'OpenAI-compatible API',
    body: 'chat · completions · embeddings · rerank · transcriptions · speech · images · models. Drop-in for any OpenAI SDK — point your client at :8080/v1 and go.',
  },
  {
    tag: 'benchmarks',
    title: 'In-dashboard benchmarks',
    body: 'hal0-bench in-tree: roster, runs, evals, and a run queue. One-click benchmark runs with per-model tok/s + latency history, all from the dashboard. Weekly scheduled runs keep the public roster honest.',
  },
  {
    tag: 'runner',
    title: 'Custom llama.cpp runner',
    body: 'We ship the latest llama.cpp branch features in our own ghcr.io/hal0ai/hal0-rocmfpx image — TurboQuant (turbo3/turbo4), ROCmFPX quantization, native FP6, and MTP spec decode on both Vulkan and ROCm. One image for the whole GPU stack.',
  },
  {
    tag: 'lifecycle',
    title: 'Typed slot state machine',
    body: 'offline → pulling → starting → warming → ready → serving ↔ idle → unloading. Atomic transitions, persisted to state.json, streamed over SSE.',
  },
  {
    tag: 'dispatcher',
    title: 'Single-flight routing',
    body: 'Registry-aware across local slots and external upstreams (OpenRouter, Anthropic, OpenAI, custom). Cold-cache prefetch coalesces a thundering herd into one HTTP call.',
  },
  {
    tag: 'probe',
    title: 'Hardware-aware everything',
    body: 'UMA pool on Strix Halo, real CPU/RAM/GPU on WSL2 / Proxmox / bare-metal. Slot-fit warnings size against the real unified pool, not a BAR carve-out.',
  },
  {
    tag: 'update',
    title: 'Cosign-keyless self-update',
    body: 'hal0 update --channel stable|nightly. Verified tarballs swap a /usr/lib/hal0/current symlink. --rollback reverts atomically. Stable + nightly channels.',
  },
  {
    tag: 'agents',
    title: 'Bundled agents + hal0-brain',
    body: 'Hermes agent with private memory, pi-coder for code tasks, and hal0-brain — a steward agent that knows the platform and helps you manage slots, models, and benchmarks from the dashboard chat.',
  },
  {
    tag: 'dashboard',
    title: 'Vue 3 + Tailwind 4 admin UI',
    body: 'Dark-by-default operator console. SSE-backed status and log tail. Capability cards group flat slots into embed / voice / image picks for one-click ops.',
  },
];

const providers = [
  {
    prov: 'llama.cpp',
    ver: 'b9279+',
    hw: 'Vulkan (default) · ROCm · CUDA',
    uses: 'chat · embed · rerank · vision',
    eps: '/v1/chat · /v1/embeddings · /v1/rerankings',
  },
  {
    prov: 'FLM (NPU trio)',
    ver: 'v0.9.44',
    hw: 'AMD XDNA NPU',
    uses: 'chat · embed · STT (concurrent)',
    eps: '/v1/chat · /v1/embeddings · /v1/audio/transcriptions',
  },
  {
    prov: 'Kokoro',
    ver: '82M',
    hw: 'CPU · Vulkan',
    uses: 'text-to-speech · 54 voices',
    eps: '/v1/audio/speech',
  },
  {
    prov: 'Qwen3-TTS',
    ver: 'v1',
    hw: 'ROCm',
    uses: 'text-to-speech · custom voice cloning',
    eps: '/v1/audio/speech',
  },
  {
    prov: 'ComfyUI',
    ver: 'v1',
    hw: 'ROCm',
    uses: 'image gen · SDXL / SD 1.5 / Flux',
    eps: '/v1/images/generations',
  },
];

const tiers = [
  {
    cls: 'first-class',
    tag: 'reference deployment',
    name: 'Ryzen AI Max+ 395',
    spec: '"Strix Halo" iGPU + XDNA NPU + 128 GB LPDDR5X-8000 unified memory',
    stats: [
      { v: '128', u: 'GB', l: 'unified · BIOS-tunable to ~96 GB GPU' },
      { v: '258', u: 'tok/s', l: 'primary + embed concurrent' },
    ],
    note: 'All published perf numbers come from this box. Q4 70B fits with massive headroom; Q4 MoE 100B+ with a 17–22B active path becomes feasible.',
  },
  {
    cls: 'first-class',
    tag: 'first-class',
    name: 'Ryzen AI Max 385 / 390',
    spec: 'Strix Halo with 64 GB unified',
    stats: [
      { v: '64', u: 'GB', l: 'unified memory' },
      { v: '~70', u: 'B', l: 'Q4 ceiling, shorter context' },
    ],
    note: 'Same install path. Every small + mid tier fits; 70B Q4 works with tighter context windows.',
  },
  {
    cls: 'supported',
    tag: 'supported',
    name: 'NVIDIA RTX 30 / 40 / 50',
    spec: '10–32 GB dedicated VRAM · CUDA llama.cpp',
    stats: [
      { v: '32', u: 'GB', l: 'RTX 5090 VRAM ceiling' },
      { v: '~30', u: 'B', l: 'Q4 comfortable' },
    ],
    note: 'Same slot lifecycle, dedicated VRAM instead of UMA — higher tok/s on small models, lower ceiling on the big ones.',
  },
  {
    cls: 'supported',
    tag: 'supported',
    name: 'AMD Radeon RX 7000',
    spec: '16–24 GB discrete · Vulkan today, ROCm in build queue',
    stats: [
      { v: '24', u: 'GB', l: '7900 XTX VRAM' },
      { v: 'Vulkan', u: '', l: 'now · ROCm soon' },
    ],
    note: 'Discrete AMD path. ROCm toolbox image is on the build list — Vulkan path works today for chat + embed.',
  },
  {
    cls: 'supported',
    tag: 'fallback',
    name: 'CPU-only x86_64',
    spec: 'Vulkan-CPU · usable for tiny models',
    stats: [
      { v: '0.5–4', u: 'B', l: 'practical model size' },
      { v: 'Qwen', u: '0.5B', l: 'the CI smoke model' },
    ],
    note: 'CI runs Qwen 0.5B here. Usable for tiny models and smoke tests, not the headline experience.',
  },
  {
    cls: 'supported',
    tag: 'supported',
    name: 'Proxmox LXC',
    spec: 'privileged container · iGPU + XDNA passthrough',
    stats: [
      { v: '0600', u: '', l: 'PVE token stored sealed' },
      { v: 'segmented', u: '', l: 'host-pressure overlay' },
    ],
    note: 'Drop a read-only PVEAuditor token into Settings and the memory bar shows physical DIMM total + a muted "Proxmox host" segment for other-tenant + ZFS ARC pressure.',
  },
];

const loadoutTabs = [
  { id: 'coding',  label: 'Coding',       count: 3 },
  { id: 'chat',    label: 'Agent / tool-use', count: 3 },
  { id: 'voice',   label: 'Voice mode',   count: 1 },
  { id: 'rag',     label: 'RAG',          count: 1 },
  { id: 'image',   label: 'Image gen',    count: 1 },
  { id: 'agentic', label: 'Platform agent', count: 1 },
  { id: 'small',   label: 'Minimal',      count: 1 },
];

const loadoutData: Record<string, Array<{ size: string; title: string; vram: string; slots: Array<{ k: string; v: string; d: string }> }>> = {
  coding: [
    { size: '~3.6 GB · snappy', title: 'Small dedicated coder', vram: '8 GB+', slots: [
      { k: 'primary', v: 'Qwopus3.5-4B-Coder-MTP', d: 'Q6_K · 3.6 GB · Qwen3.5 + Opus reasoning, MTP' },
    ]},
    { size: '~14.8 GB · daily', title: 'Workhorse 27B coder', vram: '24 GB+', slots: [
      { k: 'primary', v: 'CHADROCK3.6-27B-Coder-MTP', d: 'ROCmFP4 · 14.8 GB · agent/coder, 262K ctx, vision with mmproj' },
      { k: 'alt',     v: 'Qwopus3.6-27B-Coder-MTP', d: 'Q6_K · 22.4 GB · Opus reasoning, MTP' },
    ]},
    { size: '~31.4 GB · max', title: 'Ace Saber MoE 35B', vram: '64 GB+ UMA', slots: [
      { k: 'primary', v: 'CHADROCK-35B-Ace-Saber', d: 'ROCmFPX MoEQuality 7.07BPW · 31.4 GB · best agent/coder' },
      { k: 'alt',     v: 'CHADROCK3.6-35B-A3B-Coder', d: 'ROCmFPX MoEQuality 7.08BPW · 31.4 GB · coder variant' },
      { k: 'alt',     v: 'Qwen3.6-CROWN-35B-A3B', d: 'MTP v7 · 22.6 GB · ROCmFP4 dynamic' },
    ]},
  ],
  chat: [
    { size: '~1.1 GB · snappy', title: 'TurboQuant FPX, tiny footprint', vram: '2 GB+', slots: [
      { k: 'primary', v: 'Hal0-BRAINTRAIN-1B-FPX8-AGENT', d: 'FPX8 agent · 1.1 GB · 131K ctx' },
      { k: 'embed',   v: 'Qwen3-Embedding-0.6B', d: 'Q8_0 · 639 MB' },
    ]},
    { size: '~7.4 GB · daily', title: 'The Strix Halo daily-driver', vram: '16 GB+', slots: [
      { k: 'primary', v: 'gemma4-agentic-fable5-v2', d: 'Q4_K_M · 7.4 GB · tool-use tuned' },
      { k: 'alt',     v: 'Qwen3.5-9B-DeepSeek-V4-Flash', d: 'Q6_K · 7.6 GB · MTP, DeepSeek reasoning' },
      { k: 'npu',     v: 'FLM trio: qwen3:4b + whisper + embed', d: 'XDNA2 NPU · concurrent chat+STT+embed' },
    ]},
    { size: '~42 GB · headroom', title: 'All three modalities warm on 128 GB', vram: '96 GB UMA', slots: [
      { k: 'primary', v: 'CHADROCK-35B-Ace-Saber', d: 'ROCmFPX MoEQuality · 160 tok/s' },
      { k: 'agent',   v: 'CHADROCK-35B + hal0-brain (Ornith 9B)', d: 'platform steward + Ace Saber fallback' },
      { k: 'embed',   v: 'nomic-embed-text v1.5', d: 'Q8_0 · 274 MB · multilingual' },
    ]},
  ],
  voice: [
    { size: '~4 GB total', title: 'NPU STT + GPU TTS', vram: '8 GB+', slots: [
      { k: 'primary', v: 'gemma4-agentic-fable5-v2', d: 'Q4_K_M · 7.4 GB · fast reply' },
      { k: 'stt',     v: 'FLM Whisper v3 turbo', d: 'XDNA2 NPU · whisper-v3:turbo' },
      { k: 'tts',     v: 'Qwen3-TTS + Kokoro-82M', d: 'GPU voice cloning + CPU light' },
    ]},
  ],
  rag: [
    { size: '~20 GB', title: 'Long-context retrieval', vram: '32 GB+', slots: [
      { k: 'primary', v: 'CHADROCK3.6-27B-Coder-MTP', d: 'ROCmFP4 · 14.8 GB · 262K context · synthesis' },
      { k: 'embed',   v: 'nomic-embed-text v1.5', d: 'Q8_0 · 274 MB · top R@1' },
      { k: 'rerank',  v: 'bge-reranker-v2-m3', d: 'Q4_K_M · 438 MB · /v1/rerankings' },
    ]},
  ],
  image: [
    { size: 'varies', title: 'ComfyUI on ROCm', vram: '12 GB+', slots: [
      { k: 'img', v: 'SDXL Turbo / SD 1.5 / Flux Schnell', d: 'ComfyUI :8188 · ROCm backend' },
      { k: 'alt', v: 'Juggernaut XL v9', d: '7.1 GB · photorealistic' },
      { k: 'pro', v: 'LTX 2 19B (video)', d: '27 GB fp8 · video generation' },
    ]},
  ],
  agentic: [
    { size: '~32 GB', title: 'Platform steward + Ace Saber', vram: '64 GB+ UMA', slots: [
      { k: 'brain',   v: 'Ornith-1.0-9b (hal0-brain)', d: 'ROCmFPX STRIX_LEAN · platform steward · dashboard agent' },
      { k: 'primary', v: 'CHADROCK-35B-Ace-Saber', d: 'ROCmFPX MoEQuality · 31.4 GB · heavy tool-use fallback' },
      { k: 'mcp',     v: '/mcp/admin + /mcp/memory', d: 'Hindsight-backed memory · two-tier scope' },
    ]},
  ],
  small: [
    { size: '<1 GB', title: 'Privacy-first, CPU OK', vram: '2 GB+', slots: [
      { k: 'primary', v: 'Hal0-BRAINTRAIN-1B-STRIX_LEAN', d: 'ROCmFPX · 611 MB · FPX6/FPX8 variants available' },
      { k: 'embed',   v: 'nomic-embed-text v1.5', d: 'Q8_0 · 274 MB · multilingual' },
    ]},
  ],
};

const compareCols = ['hal0', 'ollama', 'Lemonade', 'LM Studio'];
const compareRows = [
  { lbl: 'OpenAI-compatible /v1/*', cells: ['chat · embed · rerank · STT · TTS · img', 'chat · embed', 'chat · embed', 'chat · embed · STT · TTS · img'] },
  { lbl: 'Concurrent slots', cells: ['12 systemd-managed', 'one at a time', 'one at a time', 'one at a time'] },
  { lbl: 'Slot lifecycle state machine', cells: ['typed, atomic, SSE-streamed', 'no', 'no', 'no'] },
  { lbl: 'UMA-aware hardware probe', cells: ['Strix Halo · NPU · platform-aware', 'CPU / GPU only', 'CPU / GPU only', 'CPU / GPU only'] },
  { lbl: 'XDNA NPU support', cells: ['first-class via FLM trio', 'no', 'no', 'no'] },
  { lbl: 'Dispatcher with upstreams', cells: ['OpenRouter · Anthropic · OpenAI · custom', 'no', 'no', 'no'] },
  { lbl: 'In-dashboard benchmarks', cells: ['roster · runs · evals · queue', 'no', 'no', 'no'] },
  { lbl: 'Bundled agents + MCP', cells: ['Hermes · pi-coder · hal0-brain', 'no', 'no', 'no'] },
  { lbl: 'HF model auto-update', cells: ['per-row indicator + Update all', 'manual pull', 'manual pull', 'manual pull'] },
  { lbl: 'Cosign-signed updates', cells: ['keyless OIDC + rollback', 'no', 'no', 'auto-update'] },
  { lbl: 'Headless / Linux-first', cells: ['systemd-required, headless', 'cross-platform', 'yes', 'GUI required'] },
  { lbl: 'Your data, your hardware', cells: ['yes', 'yes', 'yes', 'yes'] },
  { lbl: 'Cost per million tokens', cells: ['$0 + electricity', '$0 + electricity', '$0 + electricity', '$0 + electricity'] },
];

interface RoadmapItem { t: string; d: string; }
type RoadmapTheme = { id: string; name: string; blurb: string; shipped: RoadmapItem[]; soon: RoadmapItem[]; later: RoadmapItem[]; };
function pickStage(theme: RoadmapTheme, id: string): RoadmapItem[] {
  if (id === 'shipped') return theme.shipped;
  if (id === 'soon') return theme.soon;
  return theme.later;
}
const themes: RoadmapTheme[] = [
  {
    id: 'inference',
    name: 'Inference + providers',
    blurb: 'The /v1/* surface and the engines behind it.',
    shipped: [
      { t: 'OpenAI-compatible /v1/* API', d: 'Chat, completions, embeddings, rerank, transcriptions, speech, images. Every OpenAI SDK works unchanged against the local box.' },
      { t: 'Six-provider stack', d: 'llama.cpp (Vulkan / ROCm / CUDA) for chat/embed/rerank, FLM trio for the XDNA NPU (chat + embed + STT concurrent), Kokoro for TTS, Qwen3-TTS for GPU voice cloning, ComfyUI for image generation.' },
      { t: 'Image generation', d: 'POST /v1/images/generations served by a ComfyUI provider on ROCm. Curated SDXL Turbo / SD 1.5 / Flux Schnell with license badges.' },
      { t: 'FLM NPU trio live', d: 'Self-contained ghcr.io/hal0ai/hal0-toolbox-flm:v0.9.44. Chat + embed + STT concurrent on XDNA2 — all three modalities from one NPU slot, toggled from the dashboard.' },
      { t: 'ROCmFPX / TurboQuant', d: 'New unified ghcr.io/hal0ai/hal0-rocmfpx:c077206 image with TurboQuant turbo3/turbo4 quantization, FP6, and MTP spec decode on Vulkan and ROCm.' },
      { t: 'GPU generalization', d: 'Experimental CUDA seed profile (Vulkan fallback) and per-slot gpu_index pinning for multi-GPU hosts, beside the Strix Halo ROCm defaults.' },
      { t: 'Voice end-to-end', d: 'FLM NPU STT (Whisper v3 turbo) + Kokoro or GPU Qwen3-TTS stitched through the slot lifecycle; /v1/audio/transcriptions and /v1/audio/speech are live.' },
    ],
    soon: [],
    later: [
      { t: 'Fine-tune & LoRA hot-swap', d: 'Attach and rotate LoRAs against a warm base model without unloading the underlying weights.' },
      { t: 'Per-model rate limits & budgets', d: 'Cost-style accounting for local inference so a chatty agent can be capped without taking the whole box down.' },
    ],
  },
  {
    id: 'lifecycle',
    name: 'Slot lifecycle',
    blurb: 'Atomic transitions, real metrics, no snapshots.',
    shipped: [
      { t: 'Slot lifecycle state machine', d: 'Atomic transitions (offline → pulling → starting → warming → ready → serving), persisted and SSE-streamed.' },
      { t: 'Capability slots overlay', d: 'Embed / Voice / Img capability cards plus an NPU backend rollup, backed by /etc/hal0/capabilities.toml.' },
      { t: 'embed-rerank built-in slot', d: 'Auto-created on first enable: bge-reranker-v2-m3-q4_k_m on :8083 with --reranking. /v1/rerankings stays separate from chat.' },
      { t: 'Per-slot live metrics', d: 'GET /api/slots/metrics reads docker cgroup memory + ActiveEnterTimestampMonotonic + scraped /metrics.' },
      { t: 'Orchestrator drift reconcile', d: 'apply() reconciles capabilities.toml ↔ slots/*.toml on every call, not only on selection diff.' },
      { t: 'Stacks — declarative loadouts', d: 'Apply a whole model/slot layout atomically from one declarative file; profiles share the same portable export/import envelope.' },
      { t: 'Virtual seed profiles + tri-state MTP', d: 'Code-defined seed profiles (self-healing on upgrade) with dedicated embed/rerank lanes; MTP is a model×profile×slot Auto/On/Off decision.' },
      { t: 'In-dashboard benchmarks', d: 'hal0-bench in-tree: roster, runs, evals, run queue. One-click bench runs from the dashboard; weekly scheduled runs with tok/s + latency history.' },
    ],
    soon: [],
    later: [],
  },
  {
    id: 'install',
    name: 'Install + distribution',
    blurb: 'One command. Signed. Resumable.',
    shipped: [
      { t: 'Signed release pipeline live', d: 'releases.hal0.dev/stable.json via Cloudflare middleware proxy. A v* tag publishes the GH release; manifest reflects it in ~60s.' },
      { t: 'Cosign-signed self-update', d: 'Atomic version swap with rollback. Stable + nightly channels, GitHub OIDC-verified release tarballs.' },
      { t: 'Installer overhaul', d: 'ASCII banner + step counter, extracted preflight.sh, hal0 doctor, hardware cards + auto-populated slots/primary.toml, live-hello + QR.' },
      { t: '--models-dir install flag', d: 'install.sh --models-dir=PATH (or HAL0_MODELS_DIR, or interactive tty prompt) points a fresh install at an existing model store.' },
      { t: 'Install + version flow locked down', d: '__version__ reads from importlib.metadata. pyproject.toml is the single source of truth; installer copies the source tree into PREFIX.' },
      { t: 'Companion services + Services page', d: 'Open WebUI, ComfyUI, Hermes, Hindsight, and n8n managed from a dashboard Services page with mDNS discovery and allow-listed lifecycle actions.' },
    ],
    soon: [
      { t: 'Extensions framework', d: 'Third-party apps packaged as hal0 extensions: systemd unit + dashboard tile + healthcheck. Bring-your-own-app, lifecycle-managed.' },
      { t: 'AUR PKGBUILD & Ubuntu PPA', d: 'Native distro packages on top of the install script: pacman and apt as first-class install paths.' },
    ],
    later: [],
  },
  {
    id: 'hardware',
    name: 'Hardware + observability',
    blurb: 'Probe before you load. Memory bar that tells the truth.',
    shipped: [
      { t: 'UMA-aware probe', d: 'Detects iGPU, XDNA NPU, and unified memory pool; surfaces fit warnings inline before you load a model that won\'t fit.' },
      { t: 'Proxmox host-pressure segment', d: 'Read-only PVEAuditor token → dashboard memory bar shows physical DIMM total plus a muted host segment for other-tenant + ZFS ARC + kernel pressure.' },
      { t: 'KV-cache % gauge populates', d: 'Vulkan toolbox rebuilt against llama.cpp b9279; synthesises kv_cache_usage = max(n_prompt_tokens) / n_ctx, clamped at 1.0.' },
      { t: 'Live TTFT dashboard', d: 'Per-slot time-to-first-token sampled in the dispatcher, surfaced as a sparkline-pinned HUD readout on SlotCard.' },
      { t: 'Dashboard redesign + telemetry header', d: 'Fixed-band layout with a unified-memory hero and a combined throughput / GPU / CPU / NPU-occupancy card, all from live probes.' },
    ],
    soon: [],
    later: [
      { t: 'Multi-host federation', d: 'A slot mesh across LAN boxes: primary on the Strix Halo, embed on the workstation, all behind one /v1/* surface.' },
    ],
  },
  {
    id: 'agents',
    name: 'Agents + memory + MCP',
    blurb: 'A place for agents to live, not only to chat with.',
    shipped: [
      { t: 'MCP tool annotations', d: 'All 22 admin tools and 4 standalone memory tools ship with MCP hint annotations so SDK clients can render confirmation UIs and reason about side-effects.' },
      { t: 'Approval inbox + bell', d: 'Capital-D destructive tool calls gate through a header bell and modal inbox in the dashboard, with full CLI parity via hal0 agent approvals.' },
      { t: 'Bundled agents (Hermes, pi-coder, hal0-brain)', d: 'Three agents prewired to the local OpenAI API + MCP servers: Hermes for tool-use, pi-coder for code, and hal0-brain for platform stewardship from the dashboard chat.' },
      { t: 'HF model auto-update', d: 'Per-row update indicator detects when installed HF models have newer bytes upstream. One-click "Update all (N)" re-pulls in-place — no relocation.' },
      { t: 'Hindsight memory engine', d: 'Embedded memory with vector recall and native graph extraction; shared by default, with private per-agent banks.' },
      { t: 'Two-way MCP + hal0 mcp CLI', d: 'hal0 hosts admin + memory MCP servers and reaches external ones; manage, install, and browse servers from the CLI and dashboard.' },
    ],
    soon: [],
    later: [
      { t: 'ChatOps adapters', d: 'Slack and Matrix bridges as extensions, so you can talk to hal0 from the rooms you already live in.' },
    ],
  },
  {
    id: 'security',
    name: 'Security + UX',
    blurb: 'Off by default. Opt in when you\'re ready to expose the box.',
    shipped: [
      { t: 'Bundled OpenWebUI', d: 'Prewired chat on :3001 pointed at the local API. Zero config; the installer wires it for you.' },
      { t: 'Caddy + auth + HTTPS + mDNS', d: 'install.sh --auth=basic brings up Caddy with basic_auth, Bearer tokens for the API, automatic HTTPS, and a best-effort Avahi service file.' },
      { t: 'First-run wizard rewrite', d: 'Eight linear steps from password through hardware, primary model, capabilities, HF token, license, install, done. Conditional HF-token step.' },
      { t: 'Full-parity settings + Advanced', d: 'Every hal0.toml key editable from the dashboard, grouped with descriptions, plus a configurable slot publish host for LAN exposure.' },
    ],
    soon: [],
    later: [],
  },
];

const stages = [
  { id: 'shipped', label: 'shipped',    sub: 'running on your box today' },
  { id: 'soon',    label: 'in flight',  sub: 'next release cuts' },
  { id: 'later',   label: 'exploring',  sub: 'bets, not promises' },
];

const shipNowHighlights = [
  {
    tag: 'benchmarks',
    title: 'In-dashboard benchmarks',
    body: 'hal0-bench is now in-tree: a full benchmark engine with roster, runs, evals, and a run queue. One-click bench runs from the dashboard. Weekly scheduled runs with per-model tok/s + latency history. The public model roster is kept honest by automated regression detection.',
  },
  {
    tag: 'flm-trio',
    title: 'FLM NPU trio — chat + embed + STT',
    body: 'One NPU slot now runs chat, embeddings, and speech-to-text concurrently. Chat/ASR/Embed toggles drive the running FLM serve process. Real FastFlowLM v0.9.44 image with full model catalogue. A modality-aware readiness gate promotes each lane independently.',
  },
  {
    tag: 'brain',
    title: 'hal0-brain agent in the dashboard',
    body: 'A third seeded agent persona that stewards the platform from the dashboard chat slide-out. Its own memory namespace, hal0-heavy system prompt (slot lifecycle, model setup, benchmarking), and the dedicated brain slot. Agent-chat renders thinking blocks, markdown, and tool-call cards.',
  },
  {
    tag: 'hf-update',
    title: 'HF model auto-update detection',
    body: 'Per-row update indicator detects when installed HF models have newer bytes upstream (stored metadata.sha256 vs the current LFS oid). One-click "Update all (N)" re-pulls in-place via dest_override — no relocation, even for pre-layout and ComfyUI-subdir installs.',
  },
  {
    tag: 'turboquant',
    title: 'ROCmFPX c077206 — TurboQuant + FP6 + MTP',
    body: 'New unified llama.cpp image: TurboQuant turbo3/turbo4 quantization formats, native FP6, and MTP spec decode on both Vulkan and ROCm. One image replaces the old multi-tag ROCmFPX family. Slot-level image overrides let advanced users pin custom toolboxes.',
  },
];

---

<MarketingLayout
  title="hal0 — Strix Halo native AI inference platform for homelabs."
  description={description}
>
  <!-- HERO -->
  <header class="hero">
    <div class="shell-wide hero-grid">
      <div>
        <div class="eyebrow">
          <span class="blip"></span> {version} — shipping on every supported host
        </div>
        <h1 class="h1">
          Your Strix Halo box,<br />
          running real <span class="accent">/v1/*</span><br />
          inference.
        </h1>
        <p class="lede">
          <strong>hal0</strong> turns a Linux box — ideally a
          <strong>Ryzen AI Max+ 395</strong> with 128 GB of unified memory and
          an XDNA NPU — into a polished, OpenAI-compatible inference appliance.
          Slots, dispatcher, hardware probe, prewired chat UI, signed self-update.
          Benchmarks, agentic tool-use, and multi-modal inference — one box.
        </p>

        <div class="install" id="install">
          <div class="install-head">
            <div class="install-dots"><i></i><i></i><i></i></div>
            <div class="install-title">terminal</div>
            <div class="install-meta">bash</div>
          </div>
          <div class="install-body">
            <span class="prompt">$</span>
            <span class="cmd">curl -fsSL https://hal0.dev/install.sh | bash</span>
            <span class="caret"></span>
            <button class="copy-btn" data-copy="curl -fsSL https://hal0.dev/install.sh | bash">copy</button>
          </div>
        </div>

        <div class="cta-row">
          <a href="/docs/getting-started/" class="btn">Read the docs →</a>
          <a href="https://github.com/hal0ai/hal0" rel="noopener" class="btn ghost">★ Star on GitHub</a>
          <span class="cta-meta">Apache-2.0 · Linux + systemd · Strix Halo native</span>
        </div>

        <div class="ticker">
          <span class="tk"><span class="dot"></span> hal0-api</span>
          <span class="tk"><b>:8080</b> ready</span>
          <span class="tk-sep">·</span>
          <span class="tk">12 slots <b>managed</b></span>
          <span class="tk-sep">·</span>
          <span class="tk">UMA <b>102 GB</b></span>
          <span class="tk-sep">·</span>
          <span class="tk">NPU <b>XDNA2</b> live</span>
        </div>
      </div>

      <LiveArtifact />
    </div>
  </header>

  <!-- PERF -->
  <section class="perf" id="perf">
    <div class="shell-wide">
      <div class="perf-grid">
        <div class="perf-cell">
          <div class="num">160<span class="unit">tok/s</span></div>
          <div class="lbl">CHADROCK 35B Ace Saber</div>
          <div class="ctx">Vulkan · MoEQuality 7.07BPW · MTP</div>
        </div>
        <div class="perf-cell">
          <div class="num">&lt;200<span class="unit">ms</span></div>
          <div class="lbl">dispatch latency</div>
          <div class="ctx">both slots hot, single-flight</div>
        </div>
        <div class="perf-cell">
          <div class="num">12+<span class="unit">slots</span></div>
          <div class="lbl">chat · embed · stt · tts · img · rerank · code · brain</div>
          <div class="ctx">systemd-managed lifecycle</div>
        </div>
        <div class="perf-cell">
          <div class="num">~10<span class="unit">s</span></div>
          <div class="lbl">Qwen3 4B pull → first chat</div>
          <div class="ctx">2.4 GB · 71 tok/s · 280 ms TTFT</div>
        </div>
        <div class="perf-cell">
          <div class="num">258<span class="unit">tok/s</span></div>
          <div class="lbl">primary + embed concurrent</div>
          <div class="ctx">Strix Halo iGPU · ~9 GB GTT</div>
        </div>
        <div class="perf-cell">
          <div class="num">63<span class="unit">tok/s</span></div>
          <div class="lbl">Qwen-AgentWorld 35B A3B</div>
          <div class="ctx">Q4_0 ROCMFP4 · MoE 3B active</div>
        </div>
        <div class="perf-cell">
          <div class="num">~10<span class="unit">s</span></div>
          <div class="lbl">SDXL pull → first image</div>
          <div class="ctx">ComfyUI · 6.9 GB · 25 steps</div>
        </div>
        <div class="perf-cell">
          <div class="num">99<span class="unit">tok/s</span></div>
          <div class="lbl">MiniCPM5-1B</div>
          <div class="ctx">2.2 GB · ROCm · greedy decode</div>
        </div>
      </div>
    </div>
  </section>

  <!-- WHAT'S IN THE BOX -->
  <section class="section" id="what">
    <div class="shell-wide">
      <div class="sec-head">
        <div>
          <div class="sec-tag">/ what's in the box</div>
          <h2 class="sec-title">Not another<br />llama-server wrapper.</h2>
        </div>
        <p class="sec-sub">
          Every workload is a real systemd-managed slot with a typed lifecycle.
          The API surface covers chat <em>and</em> embed <em>and</em> rerank
          <em>and</em> STT <em>and</em> TTS <em>and</em> image gen. The dashboard
          is for operating the box — benchmarks, model management, agent chat.
        </p>
      </div>

      <div class="feat-grid">
        {features.map((f) => (
          <article class="feat-card">
            <div class="feat-tag">{f.tag}</div>
            <div class="feat-title">{f.title}</div>
            <p class="feat-body">{f.body}</p>
          </article>
        ))}
      </div>

      <div class="lifecycle-wrap">
        <div class="hr-line">SLOT LIFECYCLE — ENFORCED IN _transition()</div>
        <div class="lifecycle">
          <div class="lifecycle-track">
            <span class="state-pill done">offline</span>
            <span class="arrow">→</span>
            <span class="state-pill done">pulling</span>
            <span class="arrow">→</span>
            <span class="state-pill done">starting</span>
            <span class="arrow">→</span>
            <span class="state-pill done">warming</span>
            <span class="arrow">→</span>
            <span class="state-pill done">ready</span>
            <span class="arrow">⇄</span>
            <span class="state-pill active">serving</span>
            <span class="arrow">⇄</span>
            <span class="state-pill done">idle</span>
            <span class="arrow">→</span>
            <span class="state-pill done">unloading</span>
            <span class="arrow">→</span>
            <span class="state-pill done">offline</span>
            <span class="state-err"><span class="arrow">↳</span> <span class="state-pill err">error</span></span>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- STACK -->
  <section class="section section-tight" id="stack">
    <div class="shell-wide">
      <div class="sec-head">
        <div>
          <div class="sec-tag">/ provider stack</div>
          <h2 class="sec-title">Six providers,<br />one /v1/* surface.</h2>
        </div>
        <p class="sec-sub">
          Each provider is stateless — <code class="mono accent-mono">build_env() / start_cmd() / health() / infer()</code>.
          No global state, no shared connection pool. The picker only advertises
          a backend the slot can actually honour on your hardware. FLM runs chat,
          embeddings, and STT concurrently on the XDNA NPU.
        </p>
      </div>

      <div class="matrix">
        <div class="matrix-row head">
          <div>Provider</div>
          <div>Workloads</div>
          <div>Hardware</div>
          <div>Endpoints</div>
        </div>
        {providers.map((r) => (
          <div class="matrix-row">
            <div class="prov">{r.prov}<small>{r.ver}</small></div>
            <div class="uses">{r.uses}</div>
            <div class="hw">{r.hw}</div>
            <div class="ep">{r.eps}</div>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- HARDWARE -->
  <section class="section" id="hardware">
    <div class="shell-wide">
      <div class="sec-head">
        <div>
          <div class="sec-tag">/ hardware</div>
          <h2 class="sec-title">Strix Halo native.<br />Not Strix-Halo-<span class="accent">only</span>.</h2>
        </div>
        <p class="sec-sub">
          The probe is UMA-aware on Strix Halo and falls back to portable
          parsers on every other host. The dashboard only labels memory
          "unified" when it actually is. Linux + systemd is the only hard
          requirement.
        </p>
      </div>

      <div class="tiers">
        {tiers.map((t) => (
          <article class={`tier ${t.cls}`}>
            <div class="tier-tag">{t.tag}</div>
            <div class="tier-name">{t.name}</div>
            <div class="tier-spec">{t.spec}</div>
            <div class="tier-stats">
              {t.stats.map((s) => (
                <div class="tier-stat">
                  <div class="v">{s.v}{s.u && <span class="u">{s.u}</span>}</div>
                  <div class="l">{s.l}</div>
                </div>
              ))}
            </div>
            <div class="tier-note">{t.note}</div>
          </article>
        ))}
      </div>
    </div>
  </section>

  <!-- LOADOUTS -->
  <section class="section section-tight" id="loadouts">
    <div class="shell-wide">
      <div class="sec-head">
        <div>
          <div class="sec-tag">/ recommended loadouts</div>
          <h2 class="sec-title">Curated starting points.<br />Tweak from there.</h2>
        </div>
        <p class="sec-sub">
          Sizes are published GGUF file sizes. The slot system takes a different
          model per slot whenever you change your mind. Picks refreshed to the
          latest open-weight releases — Qwen3, Llama-4 Scout, Hermes-4, gemma-3,
          Kokoro v1.
        </p>
      </div>

      <div class="loadout-tabs" role="tablist">
        {loadoutTabs.map((t, i) => (
          <button
            class={`tab ${i === 0 ? 'active' : ''}`}
            data-tab={t.id}
            role="tab"
            aria-selected={i === 0 ? 'true' : 'false'}
          >
            {t.label} <span class="count">{t.count}</span>
          </button>
        ))}
      </div>

      {loadoutTabs.map((t, i) => (
        <div
          class={`loadouts ${i === 0 ? '' : 'is-hidden'}`}
          data-tab-panel={t.id}
          role="tabpanel"
        >
          {(loadoutData[t.id] || []).map((c) => (
            <article class="loadout">
              <div class="loadout-head">
                <span class="loadout-size">{c.size}</span>
                <span class="loadout-vram">{c.vram}</span>
              </div>
              <div class="loadout-title">{c.title}</div>
              <div class="loadout-slots">
                {c.slots.map((s) => (
                  <div class="loadout-slot">
                    <div class="k">{s.k}</div>
                    <div class="v">{s.v}<small>{s.d}</small></div>
                  </div>
                ))}
              </div>
            </article>
          ))}
        </div>
      ))}
    </div>
  </section>

  <!-- COMPARE -->
  <section class="section">
    <div class="shell-wide">
      <div class="sec-head">
        <div>
          <div class="sec-tag">/ vs. the alternatives</div>
          <h2 class="sec-title">hal0 isn't an inference engine —<br />it's the <span class="accent">orchestration</span> around one.</h2>
        </div>
        <p class="sec-sub">
          Slots survive <code class="mono accent-mono">hal0-api</code> restarts. Embeddings, rerank,
          STT, TTS, and image gen all sit behind the same
          <code class="mono accent-mono">/v1/*</code> surface. UMA-aware hardware probe and
          slot-fit warnings are first-class — not a slash command in a chat window.
        </p>
      </div>

      <div class="compare">
        <div class="cmp-row head">
          <div></div>
          {compareCols.map((c, i) => (
            <div class={i === 0 ? 'us' : ''}>{c}{i === 0 && ' (this)'}</div>
          ))}
        </div>
        {compareRows.map((r) => (
          <div class="cmp-row">
            <div class="lbl">{r.lbl}</div>
            {r.cells.map((c, j) => (
              <div class={j === 0 ? 'us-cell' : ''}>{c}</div>
            ))}
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- ROADMAP -->
  <section class="section section-tight" id="roadmap">
    <div class="shell-wide">
      <div class="sec-head">
        <div>
          <div class="sec-tag">/ roadmap</div>
          <h2 class="sec-title">One service for the<br />whole local AI stack.</h2>
        </div>
        <p class="sec-sub">
          No dates. Each themed row reads left-to-right: <span class="accent">shipped</span>,
          in flight, exploring — the closer to the left, the closer it is to
          running on your box. Tagged versions ship to
          <code class="mono accent-mono">releases.hal0.dev</code> within ~60s.
        </p>
      </div>

      <!-- Shipping Now hero -->
      <div class="ship-now">
        <div class="ship-now-head">
          <div class="ship-now-banner">
            <span class="ship-now-pulse"><span class="dot"></span> shipping now</span>
            <span class="ship-now-ver">{version}</span>
            <span class="ship-now-meta">released ~60s after tag · cosign-keyless verified · releases.hal0.dev/stable.json</span>
          </div>
          <h3 class="ship-now-headline">
            Live dashboard metrics, locked install + version flow,<br />MCP tool annotations.
          </h3>
        </div>
        <div class="ship-now-grid">
          {shipNowHighlights.map((h, i) => (
            <div class="ship-card">
              <div class="ship-card-num">{String(i + 1).padStart(2, '0')}</div>
              <div class="ship-card-tag">{h.tag}</div>
              <div class="ship-card-title">{h.title}</div>
              <div class="ship-card-body">{h.body}</div>
            </div>
          ))}
        </div>
      </div>

      <!-- Stage column headers -->
      <div class="rm-stage-head">
        <div></div>
        {stages.map((s, i) => (
          <div class={`rm-stage rm-stage-${s.id}`}>
            <div class="rm-stage-num">0{i + 1}</div>
            <div class="rm-stage-label">{s.label}</div>
            <div class="rm-stage-sub">{s.sub}</div>
          </div>
        ))}
      </div>

      <!-- Themed rows -->
      <div class="rm-grid">
        {themes.map((theme) => {
          const counts = { shipped: theme.shipped.length, soon: theme.soon.length, later: theme.later.length };
          const total = counts.shipped + counts.soon + counts.later || 1;
          return (
            <div class="rm-row">
              <div class="rm-row-head">
                <div class="rm-row-name">{theme.name}</div>
                <div class="rm-row-blurb">{theme.blurb}</div>
                <div class="rm-row-ratio">
                  <span class="rm-ratio-num">{counts.shipped}</span>
                  <span class="rm-ratio-sep">/</span>
                  <span class="rm-ratio-num">{counts.soon}</span>
                  <span class="rm-ratio-sep">/</span>
                  <span class="rm-ratio-num">{counts.later}</span>
                  <div class="rm-ratio-bar">
                    <i class="a" style={`width: ${(counts.shipped / total) * 100}%`}></i>
                    <i class="b" style={`width: ${(counts.soon / total) * 100}%`}></i>
                    <i class="c" style={`width: ${(counts.later / total) * 100}%`}></i>
                  </div>
                </div>
              </div>
              {stages.map((stage) => {
                const items = pickStage(theme, stage.id);
                return (
                  <div class={`rm-cell rm-cell-${stage.id}`}>
                    {items.length === 0 ? (
                      <div class="rm-empty">
                        <span class="rm-empty-mark">—</span>
                        <span>nothing queued</span>
                      </div>
                    ) : items.map((it) => (
                      <div class="rm-item">
                        <div class="rm-item-mark">
                          {stage.id === 'shipped' ? '✓' : stage.id === 'soon' ? '◇' : '◦'}
                        </div>
                        <div class="rm-item-body">
                          <div class="rm-item-title">{it.t}</div>
                          <div class="rm-item-desc">{it.d}</div>
                        </div>
                      </div>
                    ))}
                  </div>
                );
              })}
            </div>
          );
        })}
      </div>

      <div class="rm-footnote">
        <span class="mono accent-mono">※</span>
        <span>
          <strong>Exploring items are bets we believe in, not promises.</strong>{' '}
          Scope and order will shift as the public beta lands and we learn what people actually do with a local platform.
          <a href="https://github.com/hal0ai/hal0/issues" class="rm-footnote-link">open an issue ↗</a>
        </span>
      </div>
    </div>
  </section>

  <!-- CTA -->
  <section class="cta-section">
    <div class="shell-wide cta-inner">
      <div class="sec-tag cta-tag">/ get started</div>
      <h2 class="cta-h">Your Strix Halo box<br />is a <span class="accent">data center.</span><br />Run it like one.</h2>
      <p class="cta-sub">
        One command. Open-source. Runs on your hardware.
      </p>
      <div class="install cta-install">
        <div class="install-head">
          <div class="install-dots"><i></i><i></i><i></i></div>
          <div class="install-title">terminal</div>
          <div class="install-meta">bash</div>
        </div>
        <div class="install-body">
          <span class="prompt">$</span>
          <span class="cmd">curl -fsSL https://hal0.dev/install.sh | bash</span>
          <span class="caret"></span>
          <button class="copy-btn" data-copy="curl -fsSL https://hal0.dev/install.sh | bash">copy</button>
        </div>
      </div>
      <div class="cta-row cta-row-centered">
        <a href="/docs/getting-started/" class="btn">Read the docs →</a>
        <a href="https://github.com/hal0ai/hal0" rel="noopener" class="btn ghost">View on GitHub ↗</a>
      </div>
      <div class="cta-pills">
        <span class="pill ok">Apache-2.0</span>
        <span class="pill">Linux + systemd</span>
        <span class="pill">no telemetry by default</span>
        <span class="pill">cosign-signed releases</span>
      </div>
    </div>
  </section>

  <script is:inline type="application/ld+json" set:html={JSON.stringify(softwareApplicationLd)} />

  <!-- Tab + copy-button interactivity. Tiny and inline; no client framework. -->
  <script is:inline>
    (() => {
      // Copy buttons
      document.querySelectorAll('button[data-copy]').forEach((btn) => {
        btn.addEventListener('click', async () => {
          const cmd = btn.getAttribute('data-copy') || '';
          try { await navigator.clipboard.writeText(cmd); } catch {}
          const original = btn.textContent;
          btn.textContent = 'copied';
          setTimeout(() => { btn.textContent = original; }, 1400);
        });
      });

      // Loadout tabs
      const tabs = document.querySelectorAll('button[data-tab]');
      const panels = document.querySelectorAll('[data-tab-panel]');
      tabs.forEach((tab) => {
        tab.addEventListener('click', () => {
          const id = tab.getAttribute('data-tab');
          tabs.forEach((t) => {
            const isActive = t === tab;
            t.classList.toggle('active', isActive);
            t.setAttribute('aria-selected', isActive ? 'true' : 'false');
          });
          panels.forEach((p) => {
            p.classList.toggle('is-hidden', p.getAttribute('data-tab-panel') !== id);
          });
        });
      });
    })();
  </script>
</MarketingLayout>

<style is:global>
  /* ============================================================
   * hal0 v2 landing — design primitives that don't fit Tailwind
   * cleanly (matrix tables, lifecycle pills, mem-bar, roadmap
   * themed rows, perf strip, slot rows). Scoped to the landing
   * via the section selectors.
   * ============================================================ */

  /* Layout shell — wider than the Starlight 6xl default */
  .shell-wide {
    max-width: 1600px;
    margin: 0 auto;
    padding: 0 32px;
  }

  /* Mono helper */
  .mono {
    font-family: var(--hal0-font-mono);
    font-feature-settings: 'zero' 1, 'ss02' 1, 'calt' 0;
    letter-spacing: -0.005em;
  }
  .accent-mono { color: var(--hal0-accent); }
  .accent { color: var(--hal0-accent); }

  /* Buttons */
  .btn {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    padding: 8px 14px;
    background: var(--hal0-accent);
    color: #0a0a0a;
    font-family: var(--hal0-font-mono);
    font-size: 12px;
    font-weight: 600;
    border-radius: 4px;
    border: 1px solid var(--hal0-accent);
    cursor: pointer;
    letter-spacing: -0.01em;
    text-decoration: none;
  }
  .btn:hover { filter: brightness(1.06); }
  .btn.ghost {
    background: transparent;
    color: var(--hal0-fg);
    border-color: var(--hal0-border);
  }
  .btn.ghost:hover { border-color: var(--hal0-fg-dim); }

  /* HERO */
  .hero {
    padding: 80px 0 64px;
    position: relative;
    overflow: hidden;
  }
  .hero-grid {
    display: grid;
    grid-template-columns: minmax(0, 1.05fr) minmax(0, 1fr);
    gap: 64px;
    align-items: start;
  }
  .eyebrow {
    font-family: var(--hal0-font-mono);
    font-size: 11px;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    color: var(--hal0-accent);
    display: inline-flex;
    align-items: center;
    gap: 10px;
    padding: 5px 10px;
    border: 1px solid color-mix(in srgb, var(--hal0-accent) 34%, transparent);
    background: var(--hal0-accent-glow);
    border-radius: 999px;
    margin-bottom: 28px;
  }
  .eyebrow .blip {
    width: 5px;
    height: 5px;
    background: var(--hal0-accent);
    border-radius: 50%;
    animation: hal0-blip 1.6s ease-in-out infinite;
  }
  @keyframes hal0-blip { 0%, 100% { opacity: 0.4; } 50% { opacity: 1; } }

  .h1 {
    font-family: var(--hal0-font-mono);
    font-weight: 600;
    font-size: clamp(44px, 6vw, 80px);
    line-height: 0.95;
    letter-spacing: -0.04em;
    margin: 0 0 20px;
    font-feature-settings: 'zero' 1;
  }

  .lede {
    font-size: 18px;
    line-height: 1.55;
    color: var(--hal0-fg-muted);
    max-width: 560px;
    margin: 0 0 32px;
    text-wrap: pretty;
  }
  .lede strong { color: var(--hal0-fg); font-weight: 500; }

  /* Install code block */
  .install {
    background: var(--hal0-bg-elevated);
    border: 1px solid var(--hal0-border);
    border-radius: 10px;
    margin-bottom: 24px;
    overflow: hidden;
  }
  .install-head {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 8px 14px;
    border-bottom: 1px solid var(--hal0-border);
    font-family: var(--hal0-font-mono);
    font-size: 11px;
    color: var(--hal0-fg-dim);
  }
  .install-dots { display: flex; gap: 6px; }
  .install-dots i {
    width: 9px;
    height: 9px;
    border-radius: 50%;
    background: var(--hal0-border-strong);
  }
  .install-title { flex: 1; text-align: center; }
  .install-meta { opacity: 0.7; }
  .install-body {
    padding: 18px 18px 18px 22px;
    font-family: var(--hal0-font-mono);
    font-size: 15px;
    color: var(--hal0-fg);
    display: flex;
    align-items: center;
    gap: 12px;
    position: relative;
  }
  .install-body .prompt { color: var(--hal0-accent); user-select: none; }
  .install-body .cmd {
    color: var(--hal0-fg);
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
    flex: 1 1 auto;
  }
  .install-body .caret {
    display: inline-block;
    width: 8px;
    height: 16px;
    background: var(--hal0-accent);
    vertical-align: middle;
    animation: hal0-caret 1s steps(2) infinite;
  }
  @keyframes hal0-caret { 50% { opacity: 0; } }
  .copy-btn {
    margin-left: auto;
    background: transparent;
    border: 1px solid var(--hal0-border);
    color: var(--hal0-fg-dim);
    font-family: var(--hal0-font-mono);
    font-size: 11px;
    padding: 5px 9px;
    border-radius: 4px;
    cursor: pointer;
  }
  .copy-btn:hover { color: var(--hal0-fg); border-color: var(--hal0-fg-dim); }

  /* Holding banner — installer overhaul */
  .holding {
    display: flex;
    align-items: center;
    gap: 14px;
    flex-wrap: wrap;
    background: var(--hal0-bg-elevated);
    border: 1px solid var(--hal0-accent);
    border-radius: 10px;
    padding: 16px 18px;
    margin-bottom: 24px;
    box-shadow: 0 0 0 1px color-mix(in srgb, var(--hal0-accent) 18%, transparent) inset;
  }
  .holding-pill {
    font-family: var(--hal0-font-mono);
    font-size: 10px;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    color: var(--hal0-accent);
    border: 1px solid var(--hal0-accent);
    border-radius: 999px;
    padding: 4px 10px;
    white-space: nowrap;
  }
  .holding-msg {
    flex: 1 1 auto;
    color: var(--hal0-fg);
    font-size: 15px;
    line-height: 1.5;
  }
  .holding-msg strong { color: var(--hal0-accent); font-weight: 600; }
  .cta-holding {
    max-width: 640px;
    margin: 0 auto 28px;
    justify-content: center;
    text-align: left;
  }

  .cta-row { display: flex; gap: 10px; align-items: center; flex-wrap: wrap; }
  .cta-row-centered { justify-content: center; }
  .cta-install { max-width: 640px; margin: 0 auto 28px; }
  .cta-meta {
    font-family: var(--hal0-font-mono);
    font-size: 11px;
    color: var(--hal0-fg-dim);
    margin-left: 8px;
  }

  /* Ticker */
  .ticker {
    margin-top: 22px;
    display: flex;
    align-items: center;
    gap: 12px;
    font-family: var(--hal0-font-mono);
    font-size: 11px;
    color: var(--hal0-fg-dim);
    padding: 10px 0;
    border-top: 1px solid var(--hal0-border);
    flex-wrap: wrap;
  }
  .ticker .tk { display: flex; align-items: center; gap: 5px; }
  .ticker .tk b { color: var(--hal0-fg); font-weight: 500; }
  .ticker .dot {
    width: 6px;
    height: 6px;
    border-radius: 50%;
    background: #6fcf97;
    box-shadow: 0 0 8px #6fcf97;
    display: inline-block;
  }
  .tk-sep { color: var(--hal0-border-strong); }

  /* Live artifact (hero right column) */
  .artifact {
    background: var(--hal0-bg-elevated);
    border: 1px solid var(--hal0-border);
    border-radius: 10px;
    overflow: hidden;
    font-family: var(--hal0-font-mono);
    position: relative;
  }
  .artifact::before {
    content: '';
    position: absolute;
    inset: 0;
    background: radial-gradient(circle at 80% 0%, var(--hal0-accent-glow), transparent 50%);
    pointer-events: none;
  }
  .art-head {
    display: flex;
    align-items: center;
    gap: 14px;
    padding: 14px 18px;
    border-bottom: 1px solid var(--hal0-border);
    font-size: 11px;
    color: var(--hal0-fg-dim);
    position: relative;
    flex-wrap: wrap;
  }
  .art-tag { color: var(--hal0-fg-muted); }
  .art-tag b { color: var(--hal0-fg); font-weight: 500; }
  .art-sep { color: var(--hal0-border-strong); }
  .art-pulse {
    margin-left: auto;
    display: flex;
    align-items: center;
    gap: 6px;
    color: #6fcf97;
  }
  .art-pulse .dot {
    width: 6px;
    height: 6px;
    border-radius: 50%;
    background: #6fcf97;
    display: inline-block;
    box-shadow: 0 0 8px #6fcf97;
  }

  .slot-row {
    display: grid;
    grid-template-columns: 100px 1fr 88px 96px 70px;
    gap: 16px;
    padding: 12px 18px;
    border-bottom: 1px solid var(--hal0-border);
    align-items: center;
    font-size: 12px;
  }
  .slot-row:last-of-type { border-bottom: none; }
  .slot-name {
    color: var(--hal0-fg);
    font-weight: 500;
    display: flex;
    align-items: center;
    gap: 7px;
  }
  .slot-name .ring { width: 7px; height: 7px; border-radius: 50%; }
  .slot-name.ready .ring { background: #6fcf97; box-shadow: 0 0 6px #6fcf97; }
  .slot-name.serving .ring {
    background: var(--hal0-accent);
    box-shadow: 0 0 6px var(--hal0-accent);
    animation: hal0-pulse 1.5s ease-in-out infinite;
  }
  .slot-name.idle .ring { background: var(--hal0-fg-dim); }
  @keyframes hal0-pulse { 50% { opacity: 0.4; } }

  .slot-model {
    color: var(--hal0-fg-muted);
    font-size: 11px;
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
  }
  .slot-state {
    font-size: 10px;
    letter-spacing: 0.04em;
    text-transform: uppercase;
    color: var(--hal0-fg-muted);
  }
  .slot-state.serving { color: var(--hal0-accent); }
  .slot-state.ready { color: #6fcf97; }
  .slot-toks {
    text-align: right;
    color: var(--hal0-fg);
    font-variant-numeric: tabular-nums;
    font-size: 13px;
  }
  .slot-toks small { color: var(--hal0-fg-dim); font-size: 10px; }
  .slot-spark {
    display: flex;
    align-items: flex-end;
    gap: 1px;
    height: 18px;
    justify-content: flex-end;
  }
  .slot-spark span { width: 3px; background: var(--hal0-accent); opacity: 0.7; }

  .art-foot {
    padding: 14px 18px;
    border-top: 1px solid var(--hal0-border);
    display: grid;
    grid-template-columns: 1fr auto;
    gap: 18px;
    align-items: center;
    font-size: 11px;
    background: var(--hal0-bg);
  }
  .art-foot-mem { display: flex; flex-direction: column; gap: 6px; }
  .mem-row {
    display: flex;
    justify-content: space-between;
    margin-bottom: 6px;
    font-size: 11px;
    color: var(--hal0-fg-muted);
  }
  .mem-total b { color: var(--hal0-fg); font-weight: 500; }
  .mem-cap { color: var(--hal0-fg-dim); }
  .mem-bar {
    position: relative;
    height: 10px;
    background: var(--hal0-border-strong);
    border-radius: 2px;
    overflow: hidden;
    display: flex;
  }
  .mem-bar i { display: block; height: 100%; }
  .mem-bar i.a { background: var(--hal0-accent); }
  .mem-bar i.b { background: #6aa4ff; opacity: 0.6; }
  .mem-bar i.c { background: #6fcf97; opacity: 0.5; }
  .mem-bar i.d { background: var(--hal0-fg-dim); opacity: 0.4; }
  .mem-legend {
    display: flex;
    gap: 14px;
    color: var(--hal0-fg-dim);
    font-size: 11px;
    align-items: center;
    flex-wrap: wrap;
    margin-top: 8px;
  }
  .mem-legend .sw {
    display: inline-block;
    width: 8px;
    height: 8px;
    border-radius: 1px;
    margin-right: 6px;
    vertical-align: -1px;
  }
  .mem-legend .sw.a { background: var(--hal0-accent); }
  .mem-legend .sw.b { background: #6aa4ff; opacity: 0.6; }
  .mem-legend .sw.c { background: #6fcf97; opacity: 0.5; }
  .mem-legend .sw.d { background: var(--hal0-fg-dim); opacity: 0.4; }

  .disp-lbl { font-size: 11px; color: var(--hal0-fg-dim); margin-bottom: 4px; }
  .disp-val {
    font-family: var(--hal0-font-mono);
    font-size: 24px;
    color: var(--hal0-fg);
    letter-spacing: -0.02em;
  }
  .disp-unit { color: var(--hal0-accent); font-size: 13px; }

  /* PERF strip */
  .perf {
    border-top: 1px solid var(--hal0-border);
    border-bottom: 1px solid var(--hal0-border);
    background: var(--hal0-bg-elevated);
  }
  .perf-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    border-left: 1px solid var(--hal0-border);
  }
  .perf-cell {
    padding: 32px;
    border-right: 1px solid var(--hal0-border);
  }
  .perf-cell:nth-child(n+5) { border-top: 1px solid var(--hal0-border); background: var(--hal0-bg); }
  .perf-cell .num {
    font-family: var(--hal0-font-mono);
    font-size: 44px;
    font-weight: 500;
    letter-spacing: -0.03em;
    color: var(--hal0-fg);
    line-height: 1;
    margin-bottom: 6px;
    font-feature-settings: 'zero' 1;
  }
  .perf-cell .num .unit { color: var(--hal0-accent); font-size: 22px; margin-left: 4px; }
  .perf-cell .lbl { color: var(--hal0-fg-muted); font-size: 12px; margin-bottom: 4px; }
  .perf-cell .ctx { color: var(--hal0-fg-dim); font-size: 11px; font-family: var(--hal0-font-mono); }

  /* Sections */
  .section { padding: 96px 0; }
  .section-tight { padding-top: 64px; padding-bottom: 96px; }
  .sec-head {
    display: grid;
    grid-template-columns: minmax(0, 1fr) minmax(0, 1.4fr);
    gap: 64px;
    margin-bottom: 48px;
    align-items: end;
  }
  .sec-tag {
    font-family: var(--hal0-font-mono);
    font-size: 11px;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    color: var(--hal0-accent);
    margin-bottom: 14px;
  }
  .sec-title {
    font-family: var(--hal0-font-mono);
    font-weight: 500;
    font-size: clamp(30px, 3.2vw, 44px);
    letter-spacing: -0.03em;
    margin: 0;
    line-height: 1.05;
  }
  .sec-sub {
    color: var(--hal0-fg-muted);
    font-size: 16px;
    line-height: 1.55;
    max-width: 540px;
    text-wrap: pretty;
  }

  /* What's in the box — 6-card grid */
  .feat-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 16px;
  }
  .feat-card {
    background: var(--hal0-bg-elevated);
    border: 1px solid var(--hal0-border);
    border-radius: 10px;
    padding: 24px;
  }
  .feat-tag {
    font-family: var(--hal0-font-mono);
    font-size: 11px;
    color: var(--hal0-accent);
    margin-bottom: 14px;
    letter-spacing: 0.04em;
  }
  .feat-title {
    font-family: var(--hal0-font-mono);
    font-size: 17px;
    font-weight: 500;
    letter-spacing: -0.02em;
    margin-bottom: 10px;
    color: var(--hal0-fg);
  }
  .feat-body {
    font-size: 13px;
    color: var(--hal0-fg-muted);
    line-height: 1.55;
    text-wrap: pretty;
    margin: 0;
  }

  /* Lifecycle */
  .lifecycle-wrap { margin-top: 48px; }
  .hr-line {
    display: flex;
    align-items: center;
    gap: 16px;
    font-family: var(--hal0-font-mono);
    font-size: 10px;
    color: var(--hal0-fg-dim);
    letter-spacing: 0.1em;
    margin-bottom: 24px;
  }
  .hr-line::before,
  .hr-line::after {
    content: '';
    flex: 1;
    height: 1px;
    background: var(--hal0-border);
  }
  .lifecycle {
    background: var(--hal0-bg-elevated);
    border: 1px solid var(--hal0-border);
    border-radius: 10px;
    padding: 32px;
    font-family: var(--hal0-font-mono);
    overflow-x: auto;
  }
  .lifecycle-track {
    display: flex;
    gap: 4px;
    align-items: center;
    min-width: 800px;
    flex-wrap: nowrap;
  }
  .state-pill {
    padding: 6px 10px;
    border: 1px solid var(--hal0-border);
    border-radius: 4px;
    font-size: 11px;
    color: var(--hal0-fg-muted);
    background: var(--hal0-bg);
    white-space: nowrap;
  }
  .state-pill.active {
    color: var(--hal0-accent);
    border-color: color-mix(in srgb, var(--hal0-accent) 34%, transparent);
    background: var(--hal0-accent-glow);
  }
  .state-pill.done { color: var(--hal0-fg); }
  .state-pill.err {
    color: #ef6b6b;
    border-color: rgba(239, 107, 107, 0.3);
    background: rgba(239, 107, 107, 0.08);
  }
  .arrow { color: var(--hal0-fg-dim); font-size: 11px; }
  .state-err {
    margin-left: auto;
    display: flex;
    align-items: center;
    gap: 6px;
    font-size: 11px;
    color: #ef6b6b;
  }

  /* Provider matrix */
  .matrix {
    border: 1px solid var(--hal0-border);
    border-radius: 10px;
    overflow: hidden;
    font-family: var(--hal0-font-mono);
    background: var(--hal0-bg-elevated);
  }
  .matrix-row {
    display: grid;
    grid-template-columns: 140px 1fr 220px 240px;
    border-bottom: 1px solid var(--hal0-border);
    font-size: 13px;
  }
  .matrix-row:last-child { border-bottom: none; }
  .matrix-row.head {
    background: var(--hal0-bg);
    font-size: 10px;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    color: var(--hal0-fg-dim);
  }
  .matrix-row > div {
    padding: 18px 20px;
    border-right: 1px solid var(--hal0-border);
    display: flex;
    align-items: center;
  }
  .matrix-row > div:last-child { border-right: none; }
  .matrix-row .prov { color: var(--hal0-fg); font-weight: 500; }
  .matrix-row .prov small { color: var(--hal0-fg-dim); font-weight: 400; margin-left: 8px; }
  .matrix-row .uses { color: var(--hal0-fg-muted); }
  .matrix-row .hw { color: var(--hal0-fg-muted); }
  .matrix-row .ep { color: var(--hal0-accent); font-size: 12px; }

  /* Hardware tiers */
  .tiers {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 16px;
  }
  .tier {
    background: var(--hal0-bg-elevated);
    border: 1px solid var(--hal0-border);
    border-radius: 10px;
    padding: 24px;
    position: relative;
    overflow: hidden;
  }
  .tier.first-class { border-color: color-mix(in srgb, var(--hal0-accent) 40%, transparent); }
  .tier.first-class::before {
    content: '';
    position: absolute;
    top: 0; left: 0; right: 0;
    height: 2px;
    background: var(--hal0-accent);
  }
  .tier-tag {
    font-family: var(--hal0-font-mono);
    font-size: 10px;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    color: var(--hal0-fg-dim);
    margin-bottom: 16px;
  }
  .tier.first-class .tier-tag { color: var(--hal0-accent); }
  .tier-name {
    font-family: var(--hal0-font-mono);
    font-size: 17px;
    font-weight: 500;
    letter-spacing: -0.02em;
    color: var(--hal0-fg);
    margin-bottom: 6px;
  }
  .tier-spec {
    font-family: var(--hal0-font-mono);
    font-size: 12px;
    color: var(--hal0-fg-muted);
    margin-bottom: 20px;
    line-height: 1.5;
  }
  .tier-stats {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 12px;
    margin-bottom: 18px;
  }
  .tier-stat {
    border-top: 1px solid var(--hal0-border);
    padding-top: 10px;
  }
  .tier-stat .v {
    font-family: var(--hal0-font-mono);
    font-size: 20px;
    color: var(--hal0-fg);
    font-feature-settings: 'zero' 1;
    letter-spacing: -0.02em;
  }
  .tier-stat .v .u { color: var(--hal0-accent); font-size: 12px; margin-left: 2px; }
  .tier-stat .l {
    font-size: 11px;
    color: var(--hal0-fg-dim);
    font-family: var(--hal0-font-mono);
  }
  .tier-note { font-size: 12px; color: var(--hal0-fg-muted); line-height: 1.5; }

  /* Loadouts */
  .loadout-tabs {
    display: flex;
    gap: 4px;
    margin-bottom: 28px;
    flex-wrap: wrap;
  }
  .tab {
    padding: 8px 14px;
    border: 1px solid var(--hal0-border);
    background: transparent;
    color: var(--hal0-fg-muted);
    font-family: var(--hal0-font-mono);
    font-size: 12px;
    border-radius: 4px;
    cursor: pointer;
    display: inline-flex;
    align-items: center;
    gap: 8px;
  }
  .tab:hover { color: var(--hal0-fg); border-color: var(--hal0-fg-dim); }
  .tab.active {
    color: var(--hal0-accent);
    border-color: color-mix(in srgb, var(--hal0-accent) 34%, transparent);
    background: var(--hal0-accent-glow);
  }
  .tab .count { color: var(--hal0-fg-dim); font-size: 10px; }
  .tab.active .count { color: var(--hal0-accent); }

  .loadouts {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 16px;
  }
  .loadouts.is-hidden { display: none; }
  .loadout {
    background: var(--hal0-bg-elevated);
    border: 1px solid var(--hal0-border);
    border-radius: 10px;
    padding: 22px;
  }
  .loadout-head {
    display: flex;
    justify-content: space-between;
    align-items: baseline;
    margin-bottom: 6px;
  }
  .loadout-size {
    font-family: var(--hal0-font-mono);
    font-size: 10px;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    color: var(--hal0-fg-dim);
  }
  .loadout-vram {
    font-family: var(--hal0-font-mono);
    font-size: 12px;
    color: var(--hal0-accent);
  }
  .loadout-title {
    font-family: var(--hal0-font-mono);
    font-size: 16px;
    letter-spacing: -0.02em;
    color: var(--hal0-fg);
    margin-bottom: 18px;
  }
  .loadout-slots {
    display: flex;
    flex-direction: column;
    gap: 8px;
  }
  .loadout-slot {
    display: grid;
    grid-template-columns: 70px 1fr;
    gap: 12px;
    padding: 8px 0;
    border-top: 1px solid var(--hal0-border);
    font-family: var(--hal0-font-mono);
    font-size: 12px;
    align-items: baseline;
  }
  .loadout-slot:first-child {
    border-top: none;
    padding-top: 0;
  }
  .loadout-slot .k { color: var(--hal0-accent); font-size: 11px; }
  .loadout-slot .v { color: var(--hal0-fg-muted); }
  .loadout-slot .v small {
    color: var(--hal0-fg-dim);
    display: block;
    font-size: 10px;
    margin-top: 3px;
  }

  /* Comparison */
  .compare {
    border: 1px solid var(--hal0-border);
    border-radius: 10px;
    overflow: hidden;
    background: var(--hal0-bg-elevated);
    font-family: var(--hal0-font-mono);
  }
  .cmp-row {
    display: grid;
    grid-template-columns: 220px repeat(4, 1fr);
    border-bottom: 1px solid var(--hal0-border);
    font-size: 12px;
  }
  .cmp-row:last-child { border-bottom: none; }
  .cmp-row > div {
    padding: 16px 18px;
    border-right: 1px solid var(--hal0-border);
  }
  .cmp-row > div:last-child { border-right: none; }
  .cmp-row.head { background: var(--hal0-bg); }
  .cmp-row.head > div {
    font-size: 10px;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    color: var(--hal0-fg-dim);
  }
  .cmp-row.head .us { color: var(--hal0-accent); }
  .cmp-row .lbl { color: var(--hal0-fg); font-weight: 500; }
  .cmp-row .us-cell { background: var(--hal0-accent-glow); }

  /* Roadmap — ShippingNow hero */
  .ship-now {
    background: var(--hal0-bg-elevated);
    border: 1px solid color-mix(in srgb, var(--hal0-accent) 34%, transparent);
    border-radius: 10px;
    overflow: hidden;
    margin-bottom: 48px;
    position: relative;
  }
  .ship-now::before {
    content: '';
    position: absolute;
    inset: 0;
    background:
      radial-gradient(circle at 100% 0%, var(--hal0-accent-glow), transparent 55%),
      radial-gradient(circle at 0% 0%, color-mix(in srgb, var(--hal0-accent-glow) 70%, transparent), transparent 50%);
    pointer-events: none;
    opacity: 0.8;
  }
  .ship-now-head {
    padding: 36px 36px 32px;
    border-bottom: 1px solid var(--hal0-border);
    position: relative;
  }
  .ship-now-banner {
    display: flex;
    align-items: center;
    gap: 14px;
    font-family: var(--hal0-font-mono);
    font-size: 11px;
    color: var(--hal0-fg-muted);
    margin-bottom: 22px;
    flex-wrap: wrap;
  }
  .ship-now-pulse {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    padding: 4px 10px 4px 8px;
    border: 1px solid color-mix(in srgb, var(--hal0-accent) 34%, transparent);
    background: var(--hal0-accent-glow);
    border-radius: 999px;
    color: var(--hal0-accent);
    text-transform: uppercase;
    letter-spacing: 0.1em;
    font-size: 10px;
  }
  .ship-now-pulse .dot {
    width: 5px;
    height: 5px;
    background: var(--hal0-accent);
    border-radius: 50%;
    display: inline-block;
    animation: hal0-pulse 1.5s ease-in-out infinite;
  }
  .ship-now-ver {
    font-size: 13px;
    color: var(--hal0-fg);
    font-weight: 500;
    padding: 4px 10px;
    border: 1px solid var(--hal0-border);
    border-radius: 4px;
    background: var(--hal0-bg);
  }
  .ship-now-meta { color: var(--hal0-fg-dim); font-size: 11px; }
  .ship-now-headline {
    font-family: var(--hal0-font-mono);
    font-weight: 500;
    font-size: clamp(20px, 2.1vw, 30px);
    line-height: 1.15;
    letter-spacing: -0.025em;
    color: var(--hal0-fg);
    margin: 0;
    max-width: 880px;
    text-wrap: balance;
  }
  .ship-now-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    position: relative;
  }
  .ship-card {
    padding: 24px 22px 26px;
    border-right: 1px solid var(--hal0-border);
    position: relative;
  }
  .ship-card:last-child { border-right: none; }
  .ship-card-num {
    font-family: var(--hal0-font-mono);
    font-size: 11px;
    color: var(--hal0-fg-dim);
    margin-bottom: 14px;
    letter-spacing: 0.04em;
  }
  .ship-card-tag {
    font-family: var(--hal0-font-mono);
    font-size: 10px;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: var(--hal0-accent);
    margin-bottom: 10px;
  }
  .ship-card-title {
    font-family: var(--hal0-font-mono);
    font-size: 15px;
    font-weight: 500;
    color: var(--hal0-fg);
    letter-spacing: -0.02em;
    line-height: 1.25;
    margin-bottom: 10px;
  }
  .ship-card-body {
    font-size: 12.5px;
    line-height: 1.55;
    color: var(--hal0-fg-muted);
    text-wrap: pretty;
  }

  /* Roadmap — stage headers + themed rows */
  .rm-stage-head {
    display: grid;
    grid-template-columns: 260px 1fr 1fr 1fr;
    border-bottom: 1px solid var(--hal0-border);
  }
  .rm-stage {
    padding: 18px 22px 14px;
    border-left: 1px solid var(--hal0-border);
    position: relative;
  }
  .rm-stage:first-of-type { border-left: none; }
  .rm-stage-num {
    font-family: var(--hal0-font-mono);
    font-size: 10px;
    color: var(--hal0-fg-dim);
    letter-spacing: 0.08em;
    margin-bottom: 6px;
  }
  .rm-stage-label {
    font-family: var(--hal0-font-mono);
    font-size: 14px;
    font-weight: 500;
    letter-spacing: -0.01em;
    color: var(--hal0-fg);
    text-transform: lowercase;
    margin-bottom: 2px;
  }
  .rm-stage-sub {
    font-family: var(--hal0-font-mono);
    font-size: 11px;
    color: var(--hal0-fg-dim);
  }
  .rm-stage-shipped .rm-stage-label,
  .rm-stage-shipped .rm-stage-num { color: var(--hal0-accent); }
  .rm-stage-shipped::before {
    content: '';
    position: absolute;
    top: -1px; left: 0; right: 0;
    height: 2px;
    background: var(--hal0-accent);
  }

  .rm-grid {
    border: 1px solid var(--hal0-border);
    border-top: none;
    border-radius: 0 0 10px 10px;
    overflow: hidden;
    background: var(--hal0-bg-elevated);
  }
  .rm-row {
    display: grid;
    grid-template-columns: 260px 1fr 1fr 1fr;
    border-bottom: 1px solid var(--hal0-border);
  }
  .rm-row:last-child { border-bottom: none; }

  .rm-row-head {
    padding: 24px 22px;
    border-right: 1px solid var(--hal0-border);
    background: var(--hal0-bg);
    display: flex;
    flex-direction: column;
    gap: 8px;
  }
  .rm-row-name {
    font-family: var(--hal0-font-mono);
    font-size: 14px;
    font-weight: 500;
    color: var(--hal0-fg);
    letter-spacing: -0.01em;
    line-height: 1.25;
  }
  .rm-row-blurb {
    font-family: var(--hal0-font-mono);
    font-size: 11px;
    color: var(--hal0-fg-muted);
    line-height: 1.5;
    text-wrap: pretty;
    margin-bottom: 8px;
  }
  .rm-row-ratio {
    margin-top: auto;
    display: flex;
    align-items: center;
    gap: 6px;
    font-family: var(--hal0-font-mono);
    font-size: 11px;
    color: var(--hal0-fg-dim);
    flex-wrap: wrap;
  }
  .rm-ratio-num {
    color: var(--hal0-fg);
    font-variant-numeric: tabular-nums;
    font-feature-settings: 'zero' 1, 'tnum' 1;
  }
  .rm-row-ratio .rm-ratio-num:nth-of-type(1) { color: var(--hal0-accent); }
  .rm-row-ratio .rm-ratio-num:nth-of-type(2) { color: var(--hal0-fg-muted); }
  .rm-row-ratio .rm-ratio-num:nth-of-type(3) { color: var(--hal0-fg-dim); }
  .rm-ratio-sep { color: var(--hal0-fg-dim); }
  .rm-ratio-bar {
    flex-basis: 100%;
    display: flex;
    height: 3px;
    background: var(--hal0-border-strong);
    border-radius: 1px;
    overflow: hidden;
    margin-top: 4px;
  }
  .rm-ratio-bar i { display: block; height: 100%; }
  .rm-ratio-bar i.a { background: var(--hal0-accent); }
  .rm-ratio-bar i.b { background: var(--hal0-fg-muted); }
  .rm-ratio-bar i.c { background: var(--hal0-fg-dim); opacity: 0.5; }

  .rm-cell {
    padding: 20px 22px;
    border-right: 1px solid var(--hal0-border);
    display: flex;
    flex-direction: column;
    gap: 16px;
  }
  .rm-cell:last-child { border-right: none; }
  .rm-cell-shipped { background: color-mix(in srgb, var(--hal0-accent-glow) 35%, transparent); }
  .rm-cell-later { background: color-mix(in srgb, var(--hal0-bg) 50%, transparent); }

  .rm-item {
    display: grid;
    grid-template-columns: 16px 1fr;
    gap: 10px;
    align-items: flex-start;
  }
  .rm-item-mark {
    font-family: var(--hal0-font-mono);
    font-size: 11px;
    line-height: 1.5;
    text-align: center;
    width: 16px;
    color: var(--hal0-fg-dim);
  }
  .rm-cell-shipped .rm-item-mark { color: var(--hal0-accent); }
  .rm-cell-soon .rm-item-mark { color: var(--hal0-fg-muted); }
  .rm-item-title {
    font-family: var(--hal0-font-mono);
    font-size: 13px;
    font-weight: 500;
    color: var(--hal0-fg);
    letter-spacing: -0.01em;
    margin-bottom: 4px;
    line-height: 1.3;
  }
  .rm-cell-later .rm-item-title { color: var(--hal0-fg-muted); }
  .rm-item-desc {
    font-size: 12px;
    line-height: 1.5;
    color: var(--hal0-fg-muted);
    text-wrap: pretty;
  }
  .rm-cell-later .rm-item-desc { color: var(--hal0-fg-dim); }

  .rm-empty {
    display: flex;
    align-items: center;
    gap: 10px;
    color: var(--hal0-fg-dim);
    font-family: var(--hal0-font-mono);
    font-size: 11px;
    font-style: italic;
  }
  .rm-empty-mark { color: var(--hal0-border-strong); font-style: normal; }

  .rm-footnote {
    margin-top: 24px;
    padding: 18px 22px;
    border: 1px dashed var(--hal0-border);
    border-radius: 6px;
    background: var(--hal0-bg-elevated);
    display: flex;
    align-items: flex-start;
    gap: 14px;
    font-family: var(--hal0-font-mono);
    font-size: 12px;
    color: var(--hal0-fg-muted);
    line-height: 1.55;
  }
  .rm-footnote strong { color: var(--hal0-fg); }
  .rm-footnote-link { color: var(--hal0-accent); margin-left: 10px; }

  /* CTA */
  .cta-section {
    border-top: 1px solid var(--hal0-border);
    padding: 100px 0 80px;
    background: radial-gradient(circle at 50% 0%, var(--hal0-accent-glow), transparent 60%);
  }
  .cta-inner {
    text-align: center;
    max-width: 780px;
    margin: 0 auto;
  }
  .cta-tag { margin-bottom: 18px; }
  .cta-h {
    font-family: var(--hal0-font-mono);
    font-size: clamp(34px, 4vw, 56px);
    letter-spacing: -0.04em;
    line-height: 1;
    margin: 0 0 18px;
    font-weight: 500;
  }
  .cta-sub {
    color: var(--hal0-fg-muted);
    font-size: 17px;
    max-width: 580px;
    margin: 0 auto;
    text-wrap: pretty;
  }
  .cta-install {
    max-width: 580px;
    margin: 32px auto 24px;
    text-align: left;
  }
  .cta-pills {
    margin-top: 28px;
    font-family: var(--hal0-font-mono);
    font-size: 11px;
    color: var(--hal0-fg-dim);
    display: flex;
    flex-wrap: wrap;
    gap: 8px;
    justify-content: center;
  }

  .pill {
    display: inline-block;
    font-family: var(--hal0-font-mono);
    font-size: 10px;
    letter-spacing: 0.06em;
    text-transform: uppercase;
    padding: 2px 7px;
    border-radius: 3px;
    background: var(--hal0-bg-elevated);
    color: var(--hal0-fg-muted);
    border: 1px solid var(--hal0-border);
  }
  .pill.ok {
    color: #6fcf97;
    border-color: rgba(111, 207, 151, 0.3);
    background: rgba(111, 207, 151, 0.08);
  }

  /* Responsive */
  @media (max-width: 1280px) {
    .matrix-row { grid-template-columns: 130px 1fr 180px 200px; }
    .cmp-row { grid-template-columns: 200px repeat(4, 1fr); }
  }

  @media (max-width: 1080px) {
    .hero { padding-top: 56px; }
    .hero-grid { grid-template-columns: 1fr; gap: 40px; }
    .sec-head { grid-template-columns: 1fr; gap: 24px; }
    .perf-grid { grid-template-columns: repeat(2, 1fr); }
    .feat-grid,
    .tiers,
    .loadouts { grid-template-columns: 1fr; }
    .ship-now-grid { grid-template-columns: repeat(2, 1fr); }
    .ship-card { border-bottom: 1px solid var(--hal0-border); }
    .ship-card:nth-child(2) { border-right: none; }
    .rm-stage-head,
    .rm-row { grid-template-columns: 1fr; }
    .rm-stage { border-left: none; border-top: 1px solid var(--hal0-border); }
    .rm-stage:first-of-type { border-top: none; }
    .rm-row-head { border-right: none; border-bottom: 1px solid var(--hal0-border); }
    .rm-cell { border-right: none; border-bottom: 1px solid var(--hal0-border); }
    .matrix-row { grid-template-columns: 1fr; }
    .matrix-row > div { border-right: none; border-bottom: 1px solid var(--hal0-border); }
    .cmp-row { grid-template-columns: 1fr; }
    .cmp-row > div { border-right: none; }
    .slot-row { grid-template-columns: 90px 1fr 70px 80px 60px; gap: 10px; padding: 10px 14px; }
  }
</style>
